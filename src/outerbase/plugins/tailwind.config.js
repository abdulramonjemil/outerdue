/** @type {import("tailwindcss").Config} */
const Config = {
  // This is always replaced with generated string source code
  content: [],

  theme: {
    container: {
      center: true
    }
  },

  plugins: []
}

module.exports = Config
